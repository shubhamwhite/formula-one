const { registerUser, loginUser, getUserInformation, updateAvatar, getAvatar } = require('./user.controller')

module.exports = {
  registerUser,
  loginUser,
  getUserInformation,
  updateAvatar,
  getAvatar
}
